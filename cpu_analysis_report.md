# تقرير تحليل استهلاك المعالج (CPU) و Neon Compute

بعد تحليل هيكل الأكواد وقواعد البيانات ومسارات Next.js (بدون الحاجة لصلاحيات Vercel Dashboard)، تم تحديد الأسباب الجذرية الدقيقة لاستمرار استهلاك موارد Neon و Vercel رغم حظر بوتات الذكاء الاصطناعي (GPTBot). 

المشكلة تكمن في **عمليات البحث المفتوحة (Full Table Scans)**، و**الاستعلامات غير المخبأة (Uncached Queries) في المسارات الديناميكية**، و**عمليات الإدخال المستمرة (Continuous INSERTs)** مع كل تصفح.

إليك قائمة بأكثر 10 استعلامات ومسارات تكلفةً على النظام:

## 1. محرك البحث الفوري (Live Search) - **الأعلى تكلفة**
- **المسار/الدالة**: `searchProductsLive` (تعمل عبر `StorefrontHeader`)
- **الاستعلام**: `SELECT ... WHERE name ILIKE '%q%' OR description ILIKE '%q%'`
- **التكرار**: **عالي جداً** (يُنفذ مع كل حرف يكتبه المستخدم بعد 300ms).
- **تكلفة المعالج**: **حرجة جداً**. استخدام `ILIKE` للبحث داخل الـ `description` بدون فهارس نصية (مثل `pg_trgm` في Postgres) يجبر قاعدة بيانات Neon على عمل فحص كامل للجدول (Full Table Scan) مع كل حرف!
- **حالة التخزين المؤقت (Caching)**: **لا يعمل**. الدالة لا تستخدم أي نوع من التخزين المؤقت وتستدعي DB مباشرة.

## 2. متتبع الزيارات العام (`logPageVisit`)
- **المسار**: يعمل على **جميع** المسارات (عبر مكون `<PageTracker>` في الـ Client).
- **الاستعلام**: `INSERT INTO "PageVisit" ...`
- **التكرار**: **عالي جداً** (يُنفذ مع كل تغيير للمسار أو فتح أي صفحة).
- **تكلفة المعالج**: عالية. إجبار قاعدة البيانات على عملية كتابة (Write) مع كل تنقل داخل الموقع يمنع Neon من الدخول في وضع السكون ويستهلك Compute مستمر.
- **حالة التخزين المؤقت**: غير قابل للتخزين (لأنها عملية إدخال).

## 3. متتبع مشاهدات المنتجات (`logProductView`)
- **المسار**: `/product/[slug]`
- **الاستعلام**: `INSERT INTO "ProductView" ...`
- **التكرار**: عالي (مع كل زيارة لصفحة أي منتج).
- **تكلفة المعالج**: عالية. يتم استدعاؤه كإجراء خادم (Server Action) بشكل متزامن تقريباً أثناء بناء صفحة الخادم (Server Component)، مما يؤخر استجابة Vercel ويزيد حمل الكتابة على Neon.

## 4. حساب الحد الأدنى والأقصى للأسعار (Category Aggregations)
- **المسار**: `/category/[slug]`
- **الاستعلام**: `db.product.aggregate({ _min: { price: true }, _max: { price: true } })`
- **تكلفة المعالج**: عالية. تقوم قاعدة البيانات بالمرور على جميع منتجات القسم لحساب أقل وأعلى سعر.
- **حالة التخزين المؤقت**: **لا يعمل**. لأن الصفحة تستقبل `searchParams`، يعتبرها Next.js صفحة ديناميكية. دالة `React.cache()` تخزن البيانات **للطلب الواحد فقط (Per-request)** ولا تخزنها بين المستخدمين. لذا هذا الاستعلام الثقيل يُنفذ مع كل زائر.

## 5. جلب ماركات القسم (Category Brands Lookup)
- **المسار**: `/category/[slug]`
- **الاستعلام**: `SELECT * FROM "Brand" WHERE "id" IN (SELECT "brandId" FROM "Product" WHERE categoryId = ...)`
- **تكلفة المعالج**: عالية (Subqueries).
- **حالة التخزين المؤقت**: **لا يعمل**. يتم التنفيذ ديناميكياً مع كل طلب للصفحة.

## 6. استعلامات صفحة كل المنتجات (`/products`)
- **المسار**: `/products`
- **الاستعلامات**: جلب حساب إجمالي المنتجات (`count`)، جلب كل الأقسام (`db.category.findMany()`)، وأعلى/أقل سعر في المتجر بأكمله.
- **تكلفة المعالج**: عالية جداً. عمليات الـ Aggregation على مستوى الجدول بالكامل ثقيلة على Neon.
- **حالة التخزين المؤقت**: **لا يعمل**. تتأثر بـ `searchParams`.

## 7. استعلام المنتجات المشابهة (`/product/[slug]`)
- **المسار**: `/product/[slug]`
- **الاستعلام**: `db.product.findMany({ where: { categoryId: ... } })`
- **تكلفة المعالج**: متوسطة.
- **حالة التخزين المؤقت**: **لا يعمل**. في حين أن `getProduct` مغلفة بـ `cache()`، استعلام المنتجات المشابهة غير مخبأ ويتم تنفيذه على كل زيارة للمنتج.

## 8. فلترة البحث في صفحة المنتجات
- **المسار**: `/products?q=...`
- **الاستعلام**: فحص `ILIKE` في الـ `name` و `description`.
- **تكلفة المعالج**: عالية جداً (Full table scan) تماماً مثل البحث الفوري.

## 9. استعلام الماركات العمياء (`/brand/[slug]`)
- **المسار**: `/brand/[slug]`
- **الاستعلام**: `db.category.findMany()` بدون أي فلاتر.
- **تكلفة المعالج**: متوسطة، لكنها غير مبررة حيث تجلب كل أقسام المتجر من قاعدة البيانات بدلاً من جلب الأقسام المتعلقة بالماركة فقط أو تخزينها.
- **حالة التخزين المؤقت**: **لا يعمل**.

## 10. صفحة المجالات (Departments)
- **المسار**: `/department/[slug]`
- **تكلفة المعالج**: متوسطة. ورغم استخدام `React.cache()` لجلب المجال، إلا أن الصفحة تستخدم `searchParams` مما يجعلها ديناميكية وتستدعي الـ DB مع كل زائر جديد بدلاً من بناء الـ HTML مسبقاً (SSG) أو استخدام التخزين المؤقت لـ Next.js.

---

### ملخص المشكلة (الاستنتاج)
على الرغم من نجاح حظر الـ GPTBots، فإن الاستهلاك العالي لموارد **Neon** و **Vercel** ناتج عن:
1. **تسريب التخزين المؤقت**: الصفحات (مثل Categories و Products) ديناميكية بالكامل وتتصل بـ Neon مباشرة وتتجاهل Vercel Cache.
2. **البحث المفتوح غير المؤرشف**: البحث في وصف المنتجات باستخدام `ILIKE` يستهلك معالج Neon.
3. **متتبعات الزيارات المزعجة**: دوال `logPageVisit` و `logProductView` ترهق قاعدة البيانات بعمليات الإدخال (Writes) المستمرة.

**بانتظار إشارتك إذا كنت ترغب في البدء بكتابة خطة التنفيذ (`implementation_plan.md`) لمعالجة هذه المشاكل وإصلاح التخزين المؤقت.**
