# خطة التنفيذ الشاملة (Implementation Plan)

## 1. مشكلة تسجيل الدخول للإدارة (Admin Login Bug)
**السبب:** عند تسجيل الدخول من النافذة المنبثقة، يتم توجيه جميع المستخدمين افتراضياً إلى `/account`.
**الحل:** بعد نجاح `signIn` في النافذة المنبثقة، سيتم جلب بيانات الجلسة (`getSession`)؛ إذا كان دور المستخدم هو `ADMIN`، سيتم توجيهه إلى `/admin`، وإلا إلى `/account`.

## 2. آلية الطلب والدفع (Checkout & Orders)
**المتطلبات:**
- المستخدم المسجل يطلب منه العنوان ورقم الهاتف لمرة واحدة، ثم تحفظ وتظهر كخيارات في المرات القادمة (مع إمكانية تعديلها أو إضافة جديد).
- الزائر (Guest) لا يمكنه تأكيد الطلب، سيُطلب منه أولاً التسجيل أو الدخول، وبعدها يتم إرجاعه لصفحة إتمام الطلب ليُدخل العنوان ورقم الهاتف وطريقة الدفع.
**التنفيذ:** 
- تعديل صفحة إتمام الطلب (`/checkout`) للتحقق من الجلسة. إذا لم يكن مسجلاً، يُعرض زر لفتح نافذة الدخول.
- إضافة حقول `address` و `phone` لجدول المستخدم أو إضافتها لصفحة الإعدادات لتحديثها واسترجاعها تلقائياً عند الطلب.

## 3. فلاتر المتجر (Store Filters & Sorting UI)
**المتطلبات:**
- تحويل أزرار الفلترة والترتيب إلى أيقونات صغيرة أعلى كروت المنتجات.
- زر "تطبيق" (Apply) في القائمة الجانبية بدلاً من التطبيق الفوري.
- شريط سحب للسعر (Price Range Slider) ديناميكي بناءً على أقل وأعلى سعر للمنتجات المتاحة.
- تحديد متعدد (Checkboxes) للماركات.
- إخفاء الفلاتر من صفحات الأقسام الرئيسية (التي تحتوي أقسام فرعية فقط).
- تحديث المتجر عند التطبيق بدون إعادة تحميل كاملة للصفحة (`router.push(..., { scroll: false })`).
- إضافة زر "إزالة الفلاتر" داخل النافذة.
- إظهار نقطة إشعار (Badge) على أيقونة الفلتر إذا كان هناك فلتر مفعل.

## 4. تصميم كروت المنتجات والهيدر وتنبيهات السلة (UI/UX Tweaks)
**المنتجات:** إخفاء زر "الإضافة السريعة للسلة" من كروت المنتجات في الجوال.
**تنبيه السلة (Toast):** إصلاح ظهور التنبيه (Sonner Toaster) في الجوال بحيث يكون بالمنتصف بدون ضبابية جانبية مشوهة.
**السلة:** إزالة الخلفية الرمادية من "ملخص الطلب"، وتصغير حجم عناصر الطلب، وجعله يفتح كقائمة منسدلة (Collapsible) في الجوال.
**الهيدر:** 
- إزالة أيقونة "حسابي" من الهيدر في الجوال (موجودة بالشريط السفلي).
- تكبير أيقونات السلة، القائمة الجانبية، والبحث في الجوال.
- تصغير ارتفاع الهيدر في الجوال، وتكبير الشعار.
- جعل القائمة الجانبية (Mobile Sidebar) تفتح من اليسار بدلاً من اليمين.

## 5. محرك البحث الفوري (Live Search)
**المتطلبات:** تفعيل زر البحث ليفتح حقل إدخال في الهيدر، وعند كتابة أي حرف يتم البحث الفوري (Live Search) وإظهار النتائج في قائمة منسدلة أسفله.
**التنفيذ:** إنشاء مكون `GlobalSearch` يحتوي على حقل إدخال يرسل طلبات API (Debounced) للبحث عن المنتجات وعرضها في قائمة منسدلة قابلة للنقر.

---

### User Review Required
> [!IMPORTANT]
> الخطة تحتوي على تعديلات ضخمة تشمل تصميم المتجر، آلية الطلب، ومحرك بحث جديد. هل توافق على هذه الخطة لنبدأ التنفيذ فوراً؟
