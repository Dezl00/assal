# مهام التطوير

## 1. مشكلة تسجيل الدخول للإدارة
- `[x]` التحقق من دور المستخدم (ADMIN) في `AuthModal` بعد تسجيل الدخول لتوجيهه إلى `/admin` أو `/account`.

## 2. آلية الطلب والدفع (Checkout)
- `[x]` إضافة حقول `phone` و `address` لنموذج قاعدة بيانات المستخدم إن لم تكن موجودة.
- `[x]` تعديل `/checkout` لاستخدام بيانات المستخدم المسجل وإخفاءها إذا كانت موجودة (مع إمكانية التعديل).
- `[x]` منع غير المسجلين من إتمام الطلب وعرض نافذة التسجيل لهم.

## 3. واجهة فلاتر المتجر (Filters & Sorting UI)
- `[x]` تغيير أزرار الفلترة والترتيب لتكون أيقونات صغيرة.
- `[x]` إضافة زر "تطبيق الفلتر" وزر "مسح الكل" في القائمة الجانبية (Sidebar).
- `[x]` إضافة مؤشر سحب (Slider) للتحكم في السعر مع إظهار النطاق السعري الحقيقي من المنتجات.
- `[x]` إتاحة تحديد متعدد للماركات (Checkboxes).
- `[x]` إخفاء الفلاتر وأزرار الترتيب في صفحات الأقسام الرئيسية التي تحتوي على أقسام فرعية فقط.
- `[x]` إظهار نقطة إشعار حمراء على أيقونة الفلتر عند تفعيله.

## 4. تحسينات تجربة المستخدم (UI/UX Mobile)
- `[x]` إخفاء زر الإضافة السريعة للسلة من بطاقات المنتجات في الجوال.
- `[x]` إصلاح موقع وضبابية إشعار السلة (Toast) في الجوال.
- `[x]` تصميم ملخص الطلب في السلة: إزالة الخلفية الرمادية، تصغير الحجم، وجعله قائمة منسدلة في الجوال.
- `[x]` الهيدر: تكبير الشعار، تصغير الارتفاع، تغيير اتجاه فتح القائمة الجانبية لليسار، تكبير الأيقونات وإزالة أيقونة حسابي من هيدر الجوال.

## 5. محرك البحث الفوري (Live Search)
- `[x]` تفعيل زر البحث لفتح شريط إدخال.
- `[x]` البحث المباشر دون الحاجة لإعادة تحميل الصفحة.
- `[x]` إظهار النتائج بقائمة منسدلة فورية تحت شريط البحث مع صورة المنتج والسعر والاسم.

## 6. تحسين الروابط وكروت المنتجات
- `[x]` تقصير روابط صفحات المنتجات لتصبح `/product/[slug]` بدلاً من `/product/[category]/[slug]`.
- `[x]` التأكد من أن روابط الماركات قصيرة وجميلة `/brand/[slug]`.
- `[x]` تحسين شكل كروت المنتجات المشابهة (Related Products) والمميزة (Featured).
